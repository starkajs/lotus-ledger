/** Default window for `npm run sync:woocommerce-orders` (CLI). */
export const WOOCOMMERCE_ORDER_SYNC_DAYS = 90;

/** Default window for Sync from WooCommerce in the app UI and integration cron. */
export const WOOCOMMERCE_ORDER_APP_SYNC_DAYS = 30;

/** Hard cap for automated sync windows (cron / app). */
export const WOOCOMMERCE_ORDER_SYNC_DAYS_MAX = 90;
